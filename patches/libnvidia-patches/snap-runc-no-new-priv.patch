diff --git a/src/utils.c b/src/utils.c
index 0adc59a..b43daac 100644
--- a/src/utils.c
+++ b/src/utils.c
@@ -929,8 +929,6 @@ perm_drop_privileges(struct error *err, uid_t uid, gid_t gid, bool drop_groups)
                 errno = EPERM;
                 goto fail;
         }
-        if (prctl(PR_SET_NO_NEW_PRIVS, 1, 0, 0, 0) < 0)
-                goto fail;
         return (0);
 
  fail:
